#include <iostream>
#include <vector>
using namespace std;

int main() 
{
    vector<int> v;
    vector<int> h;
    int ocjene=0, n;
    cin >> n;
    for (int i = 0; i < ocjene; i++)
    {
        cin >> ocjene;
        v.push_back(ocjene);
    }
    for (int i = 0; i < ocjene; i++)
    {
        cin >> ocjene;
        h.push_back(ocjene);
    }
    for (int i = 0; i < ocjene; i++)
    {
        if (v[i] < h[i])
        {
            cout << "1.G";
        }
        else if (v[i] > h[i])
        {
            cout << "1.F";
        }
        else
        {
            cout << "ISTO";
        }
    }
    return 0;
}za
